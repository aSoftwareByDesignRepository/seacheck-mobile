Vendored copy of `mobile/shared/android15-play-compliance` so SeaCheck's
standalone GitHub/GitLab checkouts can `npm ci` without a sibling `../shared`
tree. Prefer updating this tree when the fleet package changes.
